# Apptorney Backend

## Project Overview

LoopBack 3 API backend (Node 8) for Zambian case law and legislation. Deployed on AWS ECS Fargate.

## Deployment

When asked to deploy the backend, read and follow the skill at:
`~/.claude/skills/deploy-apptorney-backend/SKILL.md`

Quick trigger phrases: "deploy the backend", "push backend changes", "update the backend service"

## Task Tracking

- Cross-session task files live at `../docs/tasks/`
- Start with `../docs/tasks/README.md` and `../docs/tasks/index.md`
- When backend work is completed, update the relevant Markdown checkbox from `[ ]` to `[x]`
- Add a short completion note in the task file's `## Notes` section with the date and changed files
