# 2026-03-07 Web Fixes

## Goal

Resolve the known web app issues documented in `docs/plans/2026-03-07-web-fixes.md`.

## Tasks

- [x] Match the logged-in chat view spacing and positioning to the landing page spacing for the welcome text and text field.
- [x] Replace the side menu toggle button to a more minimalist one and ensure that collapsed side menu shows only icons. the toggle button should also be visible in the collapsed side menu.

## Notes

- 2026-03-08: Updated `web-app/src/styles.css`, `web-app/src/app/app.component.html`, `web-app/src/app/components/app-sidebar/app-sidebar.component.html`, and `web-app/src/app/components/app-sidebar/app-sidebar.component.ts`.
- 2026-03-08: Built `apptorney-web-app:local` from `web-app/Dockerfile`, stopped `lupiya-frontend` to free host port `4200`, and started `apptorney-web-app-local` on `0.0.0.0:4200->80/tcp`.
- 2026-03-08: Verified container startup via `docker logs apptorney-web-app-local` and confirmed nginx serves `index.html` with `docker exec apptorney-web-app-local sh -lc "wget -S -O - http://127.0.0.1"`.
- 2026-03-08: Follow-up positioning fix added `app-chat-view` flex sizing in `web-app/src/styles.css`, rebuilt `apptorney-web-app:local`, and replaced `apptorney-web-app-local` on `4200`.
- 2026-03-08: Follow-up spacing fix reduced the logged-in empty-chat gap in `web-app/src/styles.css`, rebuilt `apptorney-web-app:local`, and replaced `apptorney-web-app-local` on `4200`.
- 2026-03-08: Final spacing adjustment added `margin-bottom: 20px` to the logged-in empty-chat heading in `web-app/src/styles.css`, rebuilt `apptorney-web-app:local`, and replaced `apptorney-web-app-local` on `4200`.
- 2026-03-08: Moved `Subscription` and `Settings` out of the primary sidebar nav into a bottom profile submenu with `Log out`, updated the sidebar component and styles, rebuilt `apptorney-web-app:local`, and replaced `apptorney-web-app-local` on `4200`.
